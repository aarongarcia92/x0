//// Wild circles #0.

// Setup:  screen size. //
void setup() {
  size(150, 150);                // Try changing the width & height.
}

// Next frame:  circle at (x,y). //
void draw() {
  ellipse( 40,40, 60,60 );  
  size(200,200);// Try changing the (x,y) and size.
}
